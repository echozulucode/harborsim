import React from 'react';
import VfdDashboard from './VfdDashboard';

const Status = () => {
  return (
    <div style={{ position: 'absolute', top: 0, left: 0, right: 0, bottom: 0 }}>
      <VfdDashboard />
    </div>
  );
};

export default Status;
